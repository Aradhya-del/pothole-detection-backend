# Deployment TODO

## Plan Progress
- [x] Plan approved

## Steps
1. [ ] Create `backend/Procfile` for Render
2. [ ] Git add/commit backend files (app.py, Procfile, etc.)
3. [ ] Setup GitHub remote and push (new repo pathole-detection-backend)
4. [ ] Test backend: `cd backend && gunicorn app:app`
5. [ ] Deploy on Render: Link GitHub repo to new Web Service
6. [ ] Get Render URL, update Flutter `lib/main.dart` URLs
7. [ ] `flutter pub get` && `flutter build apk` for multi-device
8. [ ] Test APK on devices
9. [ ] Complete
